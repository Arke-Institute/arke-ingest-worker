{
  "name": "arke-ingest-worker",
  "version": "0.1.0",
  "description": "Cloudflare Worker for handling file uploads to R2 and batch job queueing",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:e2e": "bash tests/test-nested-directories.sh",
    "test:verify": "npx ts-node tests/verify-relationships.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20",
    "hono": "^4.6.14",
    "ulidx": "^2.4.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/node": "^22.10.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^4.47.0"
  }
}
